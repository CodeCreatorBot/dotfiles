-- Per-host overrides for caelestia's variables.lua.
--
-- Loaded via package.path = ~/.config/caelestia/?.lua in caelestia's
-- hyprland.lua and merged into their Variables table before the config
-- is built. Key names must match hypr/variables.lua upstream.
return {
  terminal = "ghostty",
  browser  = "zen",
}
